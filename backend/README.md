# Backend (Express + Postgres)

1. copy .env.example -> .env and set DATABASE_URL and PORT
2. npm install
3. npm run dev  (or npm start)

Server runs on PORT (default 4000). Endpoints:
GET    /api/stories
GET    /api/stories/:id
POST   /api/stories
PUT    /api/stories/:id
DELETE /api/stories/:id
